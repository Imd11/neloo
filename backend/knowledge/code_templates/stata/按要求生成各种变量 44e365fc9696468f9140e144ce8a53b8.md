# 按要求生成各种变量

stata生成一个变量，随机设定为1-30

```jsx
gen newvar = floor(30*uniform()) + 1
```

首先使用`uniform()`函数生成一个在0到1之间的随机数，然后乘以30，然后使用`floor()`函数向下取整，最后加上1，得到一个在1到30之间的随机整数。注意：`uniform()`函数生成的随机数包含0但不包含1，所以我们需要加上1来确保生成的随机整数在1到30之间。