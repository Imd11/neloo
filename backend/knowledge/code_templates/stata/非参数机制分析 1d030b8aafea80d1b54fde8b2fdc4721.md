# 非参数机制分析

```jsx
bootstrap r(ind_eff) r(dir_eff) , reps(1000) seed(101): sgmediation density0 , mv( tauL2 ) iv( did ) cv(lngdp second  sciexpe   scier   sto)
estat bootstrap, percentile bc   // 用百分位数表示和偏差修正的置信区间

```